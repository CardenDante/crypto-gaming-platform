import BitcoinIcon from './BitcoinIcon';
import LightningIcon from './LightningIcon';

export {
  BitcoinIcon,
  LightningIcon,
};

// Also export a default object with all icons for convenience
const Icons = {
  BitcoinIcon,
  LightningIcon,
};

export default Icons;